# --- python ---------------------------------------------------------------
*.pyc
__pycache__/
.venv/

# --- macOS ------------------------------------------------------------------
.DS_Store

# --- fleet 執行期產物 -------------------------------------------------------
# 就算有人把 FLEET_HOME 指到 repo 內（例如 FLEET_HOME=$PWD 做測試），
# 這些檔案也不會被誤 commit 出去。
profiles/
*.log
state.json
watch.pid
locks/
