# 來源:真實錄製(codex CLI worker,交完報告停下來)。內容行、路徑、issue 編號
# 都已改寫成中性佔位字串。
#
# 兩個要注意的結構:
#   * 收工訊息是 `─ Worked for 48m 53s ───`,同樣**沒有左括號**,不會命中
#     busy 的 `\([0-9]+m [0-9]+s`。
#   * codex 的輸入框提示符是 `›` 不是 `❯`,而且閒置時框裡放的是灰字提示
#     (`Summarize recent commits` 這種),不是使用者打的字。

• Ran 佔位命令
  │ 佔位輸出第一行
  │ 佔位輸出第二行
  │ … +3 lines
  └ 佔位輸出結尾

────────────────────────────────────────────────────

• 報告已寫到報告目錄。

  兩件事都做完了,其中一項還缺外部證據,
  需要先決定是否接受本機測試結果。

─ Worked for 48m 53s ───────────────────────────────


› Summarize recent commits

  gpt-5.6-luna medium · ~/w/demo · Goal achieved (14m)
