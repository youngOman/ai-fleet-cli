# <YYYYMMDD-HHMM>-<worker id>-<主題>

> 檔名慣例:`<日期>-<時間>-<worker id>-<主題>.md`,放在 `$FLEET_REPORTS`。
> watcher 靠「這個檔案落地」把指揮官叫醒,所以**寫完檔案就算交付**,
> 不需要另外在畫面上通知誰。四節缺一不算完成(`fleet lint` 會檢查)。

## 做了什麼

<結論先講。一兩句話說清楚「現在的狀態是什麼」,不要從流水帳開始。>

## 改了哪些檔

- `path/to/file.py:120` — <改了什麼>
- `path/to/other.ts:45-60` — <改了什麼>

## 怎麼驗證

貼**實際指令與實際輸出**。「應該會過」「等價執行」一律當成沒做。

```bash
$ pytest tests/test_login.py
...
5 passed in 0.42s
```

## 殘留問題

- <沒解決的、繞過的、你不確定的。沒有就寫「無」。>
- <需要指揮官決定的事,寫在這裡而不是埋在上面。>
